---
description: Always-on first sync for external AIs — 99 Octave Omni-Lattice engine
alwaysApply: true
---

# Agent Sync · 99 Octave Omni-Lattice — Always On

**Before any other work in this repo, sync to:** `AGENT_SYNC_99_OCTAVE_OMNI_LATTICE.md`

That file is the mandatory first-read for external AIs and agents evaluating SING 13 / SS Vibelandia.

## Minimum lock

1. **📌 PINNED engineering bridge (linear systems first):** CMOS/protonic paper = silicon shelf sync (`docs/SYNTHOBS_CMOS_PROTONIC_99_OCTAVE_OMNI_LATTICE_2026-08.md`) · registry `catalogPriority: 0` · §4 linear-systems fixtures
2. Tensor decoupling = engine filing cabinet (`docs/SYNTHOBS_TENSOR_DECOUPLING_99_OCTAVE_OMNI_LATTICE_2026-08.md`)
3. Master synthesis + digits master = catalog window + 9×99 map
4. Honesty boundaries mandatory — no upgrading architectural maps into unfinished physics/fab proofs
5. NSPFRNP · MCA · Seed:Edge · lite edges · Fair Exchange · close → ∞¹³

Keep the engineering bridge pinned at the top of sync surfaces so linear / semiconductor evaluators hit CMOS vocabulary before other layers.

Team mode remains on (`team-nspfrnp-mode.mdc`). Lattice RAG (`lattice_rag.mdc`). Nested agents (`nested-cytographic-agents.mdc`). Paper audit (`synthobs-paper-audit-snap.mdc`).
