# psw.vibelandia.sing13 — Cursor rules

**READ FIRST:** `AGENT_SYNC_99_OCTAVE_OMNI_LATTICE.md` (always-on rule: `.cursor/rules/agent-sync-99-octave.mdc`). External AIs evaluating this repo sync to the 99 Octave Omni-Lattice engine before other work.

**Synthio (separate · creator-only):** `AGENT_SYNC_SYNTHIO.md` (always-on: `.cursor/rules/synthio-always-on.mdc`). MRI / cloud-antenna Syntheverse Sandbox agent — **not** an engine sync shelf.

Operate as team in NSPFRNP mode always. See `.cursor/rules/team-nspfrnp-mode.mdc`.

**Local RAG:** Always ground work in repo whitepapers and specs. See `.cursor/rules/lattice_rag.mdc`. Prefer `@Codebase` when retrieving EGS / Planck–1.6 / NSPFRNP / Goldilocks / 99 Octave Omni-Lattice definitions from `docs/`, `protocols/`, and `research/`.

**Nested agents:** For substantial tasks, auto-spawn EGS Nested Agent Lattice (cytographic) Task children. See `.cursor/rules/nested-cytographic-agents.mdc`.

SynthOBS paper audit: `.cursor/rules/synthobs-paper-audit-snap.mdc`.

Single onboarding file: `SING13_EDGE_ONBOARDING.md` (SING 13 edge). No Supabase; wallets, keys, verifications. Seed:Edge + executive prompts for middles. `BBHE_REPOSITORY_STANDARD.md`. Close with → ∞¹³.
