# `/engine` — Snap compiler

- **`ofc-snap.js`** — browser-loadable Snap stub: φ folding, 8.75 Hz / 18.9 Hz safety narrative, dual JSON artifacts (firmware template + musical score).

The **Look under the hood** surface (`interfaces/look-under-the-hood.html`) is now a narrative engine-room page with whitepaper links; it no longer embeds the browser compile. Run `ofc-snap.js` from your own page or read **`docs/JJ_SNAP_OFC_WHITEPAPER.md`** via `interfaces/whitepaper-surface.html`.
