/**
 * Infinite Octaves Omniversal Lattice Chat — AI stack catalog layer.
 *
 * CEO-facing product spine: where the layer sits, what it delivers without
 * forcing teams to learn the math, and what is impossible without it.
 * Honesty: Soft Story / catalog architecture — not finished physics or fab proof.
 */

export const LATTICE_CATALOG_LAYER = Object.freeze({
  id: 'ai-catalog-layer',
  kicker: 'AI stack · Catalog layer',
  title: 'Infinite Octaves Omniversal Lattice Chat',
  lead:
    'A mediation layer between your models and your data — easier access, clearer outcomes for teams and customers, and Goldilocks operating room your board can feel without learning the math.',
  stackLine:
    'Models &amp; agentic IDEs → <strong>Infinite Octaves catalog layer</strong> → ship doors, apps &amp; data',
  ctaPrimary: { href: '/lattice#ai-catalog-layer-intro', label: 'See where it sits' },
  ctaChat: { href: '/lattice-chat', label: 'Open Lattice Chat' },
  ctaPaper: {
    href: '/ship-blog/infinite-octave-ai-catalog-layer',
    label: 'Plain-language note',
  },
  ctaReading: {
    href: '/reading-room?category=lattice-catalog',
    label: 'Reading Room · this category',
  },
  honesty:
    'Catalog architecture for agentic coordination. Not a finished physics proof and not a promise that GPUs disappear. Human emergency still outranks algorithms.',
});

/** Plain-language copy constants for editors weaving catalog layer into native surface copy. */
export const LATTICE_CATALOG_LAYER_CONTENT = Object.freeze({
  stackLinePlain:
    'Models & agentic IDEs → Infinite Octaves catalog layer → ship doors, apps & resonant data',
  oneLiner:
    'The Infinite Octaves catalog layer sits between models and data so your people see better answers and safer defaults — without studying Φ_EGS or rewriting the stack.',
  ceoPromise:
    'Customers and frontline teams feel the upgrade. Engineers keep their tools. Leadership gets a with/without sandbox before any rip-and-replace.',
  accessLine:
    'Access is a briefing door and a container — not a six-month platform migration.',
  transparencyLine:
    'Your users never need the math. They see faster, clearer, cooler outcomes. The catalog stays inspectable for your architects.',
  honestyShort:
    'Catalog architecture for agentic coordination — not a finished physics proof, and not a promise that GPUs disappear.',
});

/**
 * Five key architectural innovations featured wherever the catalog layer is introduced.
 * Guest labels map to nearest Infinite Octaves corpus shelves (honesty-bound Soft Story).
 */
export const CATALOG_LAYER_INNOVATIONS = Object.freeze([
  Object.freeze({
    id: 'volumetric-interference',
    title: 'Volumetric interference processing',
    guestLabel: 'Prime-indexed volumetric vaults · zero ISI grammar',
    delivers:
      'Files enterprise context into prime-indexed volumetric vaults so signals stay orthogonal — denser memory with far less crosstalk than flat vector piles.',
    withoutIt:
      'Without it you stay on RS/LDPC parity taxes and B⁺-tree lookup habits; deep context keeps colliding and token bloat wins every quarter.',
    href: '/ship-blog/prime-indexed-volumetric-storage',
    hrefLabel: 'Volumetric storage note',
    honestyBite: 'Catalog framing — not a JEDEC drop-in or measured ECC retirement.',
  }),
  Object.freeze({
    id: 'golden-ratio-cataloging',
    title: 'Golden ratio cataloging',
    guestLabel: 'Φ_EGS ≈ 1.618 recursive nesting',
    delivers:
      'Nests knowledge as closed-form harmonic standing-wave maps keyed by Φ_EGS — a third path between brute-force burn and regulatory freeze.',
    withoutIt:
      'Without it leadership is stuck choosing more GPUs or more bureaucracy; there is no ordered mediation layer for context and agents.',
    href: '/infinite-octave-egs-catalog',
    hrefLabel: 'EGS Catalog brochure',
    honestyBite: 'Design / catalog key — not a CODATA replacement for ħ, c, or G.',
  }),
  Object.freeze({
    id: 'prime-container-storage',
    title: 'Prime container geometrical storage',
    guestLabel: 'Odd-prime containment vaults · geometry sketch',
    delivers:
      'Holds sequences in odd-prime geometrical vaults so hard scientific and ops problems can be filed as resonance sketches instead of endless search.',
    withoutIt:
      'Without it you only have statistical predictors and brute-force search — months of burn for approximations your board cannot audit as architecture.',
    href: '/ship-blog/protein-folding-prime-container',
    hrefLabel: 'Prime-container note',
    honestyBite: 'Architecture Soft Story — not CASP gold, not clinical, not AlphaFold retirement.',
  }),
  Object.freeze({
    id: 'holographic-rhyme-wiring',
    title: 'Holographic rhyme wiring',
    guestLabel: 'Four-pillar rhyme · Tier C wiring lattices',
    delivers:
      'Wires repeating, self-similar, self-correcting, recursive routes so agents follow brief circuits instead of re-pasting fat dumps across every tool.',
    withoutIt:
      'Without it “holographic” is marketing wallpaper; narrative shelves stay inert and every team reinvent the same prompt pile.',
    href: '/ship-blog/holographic-rhyme',
    hrefLabel: 'Holographic rhyme note',
    hrefSecondary: '/ship-blog/tier-c-holographic-wiring',
    hrefSecondaryLabel: 'Tier C wiring',
    honestyBite: 'Catalog motion + wiring grammar — not a claim that prose causes physics.',
  }),
  Object.freeze({
    id: 'goldilocks-net-zero',
    title: 'Goldilocks (Net Zero)',
    guestLabel: 'Just-right operating band · Net Zero crystal',
    delivers:
      'Keeps the stack in a just-right band — cool enough to avoid thermal and spend runaway, warm enough to keep innovation moving — aimed at Net Zero dissipation as an operating target.',
    withoutIt:
      'Without it you oscillate between “too hot” (burn and drift) and “too cold” (freeze and moats); there is no shared just-right control surface for the enterprise.',
    href: '/ship-blog/holographic-singularity-crystal',
    hrefLabel: 'Net Zero crystal note',
    hrefSecondary: '/ship-blog/zero-octave-y-goldilocks',
    hrefSecondaryLabel: 'Y Goldilocks',
    honestyBite: 'Operating-band Soft Story — not a claim of literal SI zero watts or finished climate accounting.',
  }),
]);

/**
 * Security fabric — woven into the catalog layer at every scale, 24×365.
 * Not a bolted scanner: sovereign edges, inspectable mediation, human emergency override.
 */
export const CATALOG_LAYER_SECURITY_FABRIC = Object.freeze({
  id: 'security-fabric',
  title: 'Security woven into the fabric · 24×365',
  lead:
    'Security is not a weekend add-on. The catalog layer carries sovereignty, inspectability, and human override in the same grain as routing and filing.',
  delivers: Object.freeze([
    Object.freeze({
      level: 'Edge & keys',
      how: 'Lite edges keep wallets, keys, and verifications on-device; center stays pipes only — no Supabase-shaped custody surprise.',
    }),
    Object.freeze({
      level: 'Mediation layer',
      how: 'The catalog sits as an interceptor you can audit: schemas nest under Φ_EGS, prompts compress, and agents follow wired briefs instead of opaque scrapes.',
    }),
    Object.freeze({
      level: 'Teams & customers',
      how: 'Users see safer defaults and clearer answers; they never need the math. Architects still open the filing map.',
    }),
    Object.freeze({
      level: 'Board & emergency',
      how: 'Human emergency outranks algorithms 24×365. Fair Exchange and honor rails stay voluntary belonging — not dark-pattern lock-in.',
    }),
  ]),
  withoutIt:
    'Without fabric-level security you bolt scanners onto a treadmill: more filters after the burn, more policy after the breach, and still no inspectable mediation grain.',
  honesty:
    'Architecture Soft Story for domestic sovereignty and inspectable mediation — not a Common Criteria cert, penetration-test report, or promise of unbreakable systems.',
});

export const CATALOG_LAYER_INNOVATIONS_HONESTY =
  'Five catalog-layer innovations plus security fabric as Infinite Octaves Soft Story architecture — not finished physics, fab, or JEDEC proofs. Human emergency still outranks algorithms.';

/** Executive sandbox metrics: with vs without the catalog layer (demo grammar, not measured SI). */
export const CATALOG_LAYER_SANDBOX_FUNCTIONS = Object.freeze([
  Object.freeze({
    id: 'exec-brief',
    label: 'Board brief latency',
    without: 'Scattered decks · 3–5 days',
    withLayer: 'One wired brief · same morning',
  }),
  Object.freeze({
    id: 'ops-risk',
    label: 'Ops risk scan',
    without: 'Tool sprawl · missed cross-links',
    withLayer: 'Standing-wave map · visible gaps',
  }),
  Object.freeze({
    id: 'customer-answer',
    label: 'Customer answer quality',
    without: 'Hallucination tax · re-prompts',
    withLayer: 'Nested context · quieter drift',
  }),
  Object.freeze({
    id: 'spend-heat',
    label: 'Spend & thermal band',
    without: 'Always “too hot” or frozen',
    withLayer: 'Goldilocks Net Zero target',
  }),
  Object.freeze({
    id: 'security-posture',
    label: 'Security posture',
    without: 'Bolted filters after the fact',
    withLayer: 'Fabric · edge keys · human override',
  }),
]);

/** Compact navy–gold CSS for catalog-layer innovation grids (no purple glow). */
export function renderCatalogLayerInnovationsCss(classPrefix = 'cat-innov') {
  return `
    .${classPrefix} { margin: 1.5rem 0 0; }
    .${classPrefix}__lead { margin: 0 0 1rem; max-width: 42rem; line-height: 1.45; }
    .${classPrefix}__grid {
      display: grid;
      gap: 0.85rem;
      grid-template-columns: 1fr;
    }
    @media (min-width: 720px) {
      .${classPrefix}__grid { grid-template-columns: 1fr 1fr; }
    }
    @media (min-width: 1100px) {
      .${classPrefix}__grid { grid-template-columns: repeat(3, 1fr); }
    }
    .${classPrefix}__card {
      margin: 0;
      padding: 0.9rem 1rem 1rem;
      border: 1px solid rgba(201, 162, 39, 0.35);
      background: rgba(8, 18, 40, 0.55);
      border-radius: 2px;
    }
    .${classPrefix}__title {
      margin: 0 0 0.25rem;
      font-size: 1.05rem;
      color: #fff8e7;
      line-height: 1.25;
    }
    .${classPrefix}__label {
      margin: 0 0 0.55rem;
      font-size: 0.72rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: #c9a227;
    }
    .${classPrefix}__delivers,
    .${classPrefix}__without {
      margin: 0 0 0.45rem;
      font-size: 0.9rem;
      line-height: 1.4;
      color: rgba(244, 239, 227, 0.9);
    }
    .${classPrefix}__links { margin: 0.35rem 0; font-size: 0.82rem; }
    .${classPrefix}__links a { color: #c9a227; margin-right: 0.75rem; }
    .${classPrefix}__honesty,
    .${classPrefix}__rail {
      margin: 0.35rem 0 0;
      font-size: 0.72rem;
      line-height: 1.35;
      color: rgba(244, 239, 227, 0.55);
    }
    .${classPrefix}__security {
      margin: 1.25rem 0 0;
      padding: 1rem 1.05rem 1.1rem;
      border: 1px solid rgba(201, 162, 39, 0.4);
      background:
        linear-gradient(135deg, rgba(201, 162, 39, 0.1), transparent 55%),
        rgba(8, 18, 40, 0.72);
    }
    .${classPrefix}__security h3 {
      margin: 0 0 0.35rem;
      font-size: 1.1rem;
      color: #fff8e7;
    }
    .${classPrefix}__security-lead {
      margin: 0 0 0.75rem;
      font-size: 0.92rem;
      line-height: 1.4;
      color: rgba(244, 239, 227, 0.9);
      max-width: 46rem;
    }
    .${classPrefix}__security-list {
      margin: 0;
      padding: 0;
      list-style: none;
      display: grid;
      gap: 0.55rem;
    }
    @media (min-width: 720px) {
      .${classPrefix}__security-list { grid-template-columns: 1fr 1fr; }
    }
    .${classPrefix}__security-list li {
      margin: 0;
      padding: 0.45rem 0.55rem;
      border-left: 2px solid rgba(201, 162, 39, 0.45);
      font-size: 0.86rem;
      line-height: 1.35;
      color: rgba(244, 239, 227, 0.88);
    }
    .${classPrefix}__security-list strong {
      display: block;
      color: #c9a227;
      font-size: 0.72rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      margin-bottom: 0.2rem;
    }
  `.replace(/\n\s+/g, '\n');
}

function renderSecurityFabricHtml(classPrefix = 'cat-innov') {
  const S = CATALOG_LAYER_SECURITY_FABRIC;
  const items = S.delivers
    .map(
      (d) =>
        `<li><strong>${d.level}</strong>${d.how}</li>`,
    )
    .join('\n');
  return `<aside class="${classPrefix}__security" id="${S.id}" aria-label="${S.title}">
  <h3>${S.title}</h3>
  <p class="${classPrefix}__security-lead">${S.lead}</p>
  <ul class="${classPrefix}__security-list">
${items}
  </ul>
  <p class="${classPrefix}__without"><strong>Cannot achieve without it:</strong> ${S.withoutIt}</p>
  <p class="${classPrefix}__honesty">${S.honesty}</p>
</aside>`;
}

/** Compact HTML section for guest surfaces (navy–gold; no purple glow). */
export function renderCatalogLayerInnovationsHtml({
  headingId = 'catalog-layer-innovations-h',
  sectionId = 'catalog-layer-innovations',
  classPrefix = 'cat-innov',
  includeSecurity = true,
} = {}) {
  const items = CATALOG_LAYER_INNOVATIONS.map((inn) => {
    const secondary = inn.hrefSecondary
      ? ` <a href="${inn.hrefSecondary}">${inn.hrefSecondaryLabel}</a>`
      : '';
    return `<article class="${classPrefix}__card" id="${inn.id}">
  <h3 class="${classPrefix}__title">${inn.title}</h3>
  <p class="${classPrefix}__label">${inn.guestLabel}</p>
  <p class="${classPrefix}__delivers"><strong>Delivers:</strong> ${inn.delivers}</p>
  <p class="${classPrefix}__without"><strong>Cannot achieve without it:</strong> ${inn.withoutIt}</p>
  <p class="${classPrefix}__links"><a href="${inn.href}">${inn.hrefLabel}</a>${secondary}</p>
  <p class="${classPrefix}__honesty">${inn.honestyBite}</p>
</article>`;
  }).join('\n');

  const security = includeSecurity ? `\n${renderSecurityFabricHtml(classPrefix)}\n` : '';

  return `<!-- CATALOG_LAYER_INNOVATIONS_START -->
<section class="${classPrefix}" id="${sectionId}" aria-labelledby="${headingId}">
  <h2 id="${headingId}">What this layer files that linear stacks cannot</h2>
  <p class="${classPrefix}__lead">Five architectural moves — plus security woven into the fabric 24×365. Your teams feel the upgrade; they do not need the math.</p>
  <div class="${classPrefix}__grid">
${items}
  </div>${security}
  <p class="${classPrefix}__rail">${CATALOG_LAYER_INNOVATIONS_HONESTY}</p>
</section>
<!-- CATALOG_LAYER_INNOVATIONS_END -->`;
}

/** Visual stack strip: where the catalog layer sits. */
export function renderCatalogLayerStackHtml({ classPrefix = 'cat-stack' } = {}) {
  return `<div class="${classPrefix}" id="catalog-layer-stack" aria-label="Where the catalog layer sits on the AI stack">
  <ol class="${classPrefix}__steps">
    <li><span>Models &amp; agentic IDEs</span></li>
    <li class="${classPrefix}__here"><span>Infinite Octaves catalog layer</span><em>You install here</em></li>
    <li><span>Apps, ship doors &amp; data</span></li>
  </ol>
  <p class="${classPrefix}__note">Customers and frontline users only see better answers. Architects keep an inspectable filing map. No sixth cruise door — mediation over the five you already have.</p>
</div>`;
}

export function renderCatalogLayerStackCss(classPrefix = 'cat-stack') {
  return `
    .${classPrefix} { margin: 1rem 0 1.5rem; }
    .${classPrefix}__steps {
      list-style: none;
      margin: 0;
      padding: 0;
      display: grid;
      gap: 0.45rem;
    }
    @media (min-width: 720px) {
      .${classPrefix}__steps {
        grid-template-columns: 1fr auto 1fr auto 1fr;
        align-items: stretch;
        gap: 0;
      }
      .${classPrefix}__steps li:not(:last-child)::after {
        content: "→";
        display: flex;
        align-items: center;
        padding: 0 0.55rem;
        color: #c9a227;
        font-weight: 700;
      }
    }
    .${classPrefix}__steps li {
      margin: 0;
      padding: 0.75rem 0.9rem;
      border: 1px solid rgba(201, 162, 39, 0.28);
      background: rgba(8, 18, 40, 0.5);
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      justify-content: center;
    }
    .${classPrefix}__steps span {
      font-size: 0.92rem;
      color: #f4efe3;
      line-height: 1.3;
    }
    .${classPrefix}__here {
      border-color: rgba(201, 162, 39, 0.7);
      background:
        linear-gradient(135deg, rgba(201, 162, 39, 0.18), transparent 60%),
        rgba(12, 28, 52, 0.85);
    }
    .${classPrefix}__here em {
      font-style: normal;
      font-size: 0.68rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #c9a227;
    }
    .${classPrefix}__note {
      margin: 0.65rem 0 0;
      font-size: 0.85rem;
      line-height: 1.4;
      color: rgba(244, 239, 227, 0.7);
      max-width: 44rem;
    }
  `.replace(/\n\s+/g, '\n');
}

/** Compact CSS shared by injected bands (navy-gold cruise, not purple glow). */
export function renderLatticeCatalogLayerCss(prefix = 'lat-cat') {
  return `
    .${prefix}-layer {
      position: relative;
      z-index: 4;
      margin: 0;
      padding: 0.85rem 1.15rem 1rem;
      border-bottom: 1px solid rgba(201, 162, 39, 0.35);
      background:
        linear-gradient(105deg, rgba(8, 18, 40, 0.97) 0%, rgba(16, 42, 72, 0.94) 55%, rgba(12, 28, 52, 0.96) 100%);
      color: #f4efe3;
      font-family: "Source Serif 4", "Iowan Old Style", Palatino, Georgia, serif;
    }
    .${prefix}-layer__inner {
      max-width: 68rem;
      margin: 0 auto;
      display: grid;
      gap: 0.45rem 1.25rem;
    }
    @media (min-width: 720px) {
      .${prefix}-layer__inner {
        grid-template-columns: 1fr auto;
        align-items: end;
      }
    }
    .${prefix}-layer__kicker {
      display: inline-block;
      font-family: "DM Sans", "Avenir Next", "Segoe UI", sans-serif;
      font-size: 0.68rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: #c9a227;
      margin: 0 0 0.2rem;
    }
    .${prefix}-layer__title {
      margin: 0;
      font-size: clamp(1.05rem, 2.4vw, 1.35rem);
      font-weight: 600;
      letter-spacing: 0.01em;
      color: #fff8e7;
      line-height: 1.25;
    }
    .${prefix}-layer__lead {
      margin: 0.15rem 0 0;
      font-size: 0.95rem;
      line-height: 1.45;
      color: rgba(244, 239, 227, 0.88);
      max-width: 46rem;
    }
    .${prefix}-layer__stack {
      margin: 0.35rem 0 0;
      font-family: "DM Sans", "Avenir Next", "Segoe UI", sans-serif;
      font-size: 0.78rem;
      letter-spacing: 0.02em;
      color: rgba(201, 162, 39, 0.95);
    }
    .${prefix}-layer__ctas {
      display: flex;
      flex-wrap: wrap;
      gap: 0.45rem 0.65rem;
      align-items: center;
      justify-content: flex-start;
    }
    @media (min-width: 720px) {
      .${prefix}-layer__ctas { justify-content: flex-end; }
    }
    .${prefix}-layer__ctas a {
      font-family: "DM Sans", "Avenir Next", "Segoe UI", sans-serif;
      font-size: 0.78rem;
      letter-spacing: 0.04em;
      text-decoration: none;
      color: #081228;
      background: #c9a227;
      border: 1px solid #c9a227;
      padding: 0.42rem 0.75rem;
      border-radius: 2px;
    }
    .${prefix}-layer__ctas a:hover { background: #e0bc4a; border-color: #e0bc4a; }
    .${prefix}-layer__ctas a.${prefix}-layer__ghost {
      background: transparent;
      color: #f4efe3;
      border-color: rgba(244, 239, 227, 0.45);
    }
    .${prefix}-layer__ctas a.${prefix}-layer__ghost:hover {
      border-color: #c9a227;
      color: #c9a227;
    }
    .${prefix}-layer__honesty {
      grid-column: 1 / -1;
      margin: 0.15rem 0 0;
      font-size: 0.72rem;
      line-height: 1.35;
      color: rgba(244, 239, 227, 0.55);
    }
  `.replace(/\n\s+/g, '\n');
}

/** Full sitewide introduction band (QUESTFEST, Canvas, doors). */
export function renderLatticeCatalogLayerBandHtml({ compact = false } = {}) {
  const L = LATTICE_CATALOG_LAYER;
  const lead = compact
    ? 'Mediation between models and data — your people feel the upgrade without learning the math.'
    : L.lead;
  return `<!-- LATTICE_CATALOG_LAYER_START -->
  <aside class="lat-cat-layer" id="ai-catalog-layer" aria-label="${L.kicker}">
    <div class="lat-cat-layer__inner">
      <div class="lat-cat-layer__copy">
        <p class="lat-cat-layer__kicker">${L.kicker}</p>
        <p class="lat-cat-layer__title">${L.title}</p>
        <p class="lat-cat-layer__lead">${lead}</p>
        <p class="lat-cat-layer__stack">${L.stackLine}</p>
      </div>
      <div class="lat-cat-layer__ctas">
        <a href="${L.ctaPrimary.href}">${L.ctaPrimary.label}</a>
        <a class="lat-cat-layer__ghost" href="${L.ctaChat.href}">${L.ctaChat.label}</a>
        <a class="lat-cat-layer__ghost" href="${L.ctaPaper.href}">${L.ctaPaper.label}</a>
        <a class="lat-cat-layer__ghost" href="${L.ctaReading.href}">${L.ctaReading.label}</a>
      </div>
      <p class="lat-cat-layer__honesty">${L.honesty}</p>
    </div>
  </aside>
  <!-- LATTICE_CATALOG_LAYER_END -->`;
}

/** Marker pair used by sync scripts. */
export const LATTICE_CATALOG_LAYER_MARKERS = Object.freeze({
  start: '<!-- LATTICE_CATALOG_LAYER_START -->',
  end: '<!-- LATTICE_CATALOG_LAYER_END -->',
  styleStart: '<!-- LATTICE_CATALOG_LAYER_STYLE_START -->',
  styleEnd: '<!-- LATTICE_CATALOG_LAYER_STYLE_END -->',
});

export const CATALOG_LAYER_INNOVATIONS_MARKERS = Object.freeze({
  start: '<!-- CATALOG_LAYER_INNOVATIONS_START -->',
  end: '<!-- CATALOG_LAYER_INNOVATIONS_END -->',
  styleId: 'catalog-layer-innovations-style',
});
