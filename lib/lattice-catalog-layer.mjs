/**
 * Infinite Octave Omniversal Lattice Catalog — AI stack catalog layer.
 *
 * Product = Catalog. Lattice Chat = live Syntheverse Sandbox demo for evaluators.
 * Band HTML helpers remain for tests/archive but sitewide chrome is retired
 * in favor of native content weave. Architecture catalog: coordination fabric
 * between agentic IDEs and data — not a claim of infinite GPU-free scaling.
 */

/** Guest product name (singular Octave · Catalog). Engine pin remains Infinite Octaves. */
export const LATTICE_PRODUCT_NAME = 'Infinite Octave Omniversal Lattice Catalog';

/** Live evaluator demo of the Catalog inside Syntheverse Sandbox (`/lattice-chat`). */
export const LATTICE_CHAT_DEMO = Object.freeze({
  label: 'Lattice Chat',
  title: 'Lattice Chat · live demo',
  kicker: 'Syntheverse Sandbox · evaluator demo',
  href: '/lattice-chat',
  oneLiner:
    'Lattice Chat is the live Syntheverse Sandbox demo of Infinite Octave Omniversal Lattice Catalog — safe for evaluators to try nested routing on their own keys without touching production data.',
});

export const LATTICE_CATALOG_LAYER = Object.freeze({
  id: 'ai-catalog-layer',
  kicker: 'AI stack · Catalog layer',
  title: LATTICE_PRODUCT_NAME,
  lead:
    'The mediation layer between your models and your data — clearer briefs for teams, better answers for customers, and a calmer bill for leadership. No sixth cruise door. No new math for people who only need better outcomes.',
  stackLine:
    'Models &amp; IDEs → <strong>Infinite Octave Omniversal Lattice Catalog</strong> → ship doors &amp; enterprise data',
  ctaPrimary: { href: '/lattice#ai-catalog-layer-intro', label: 'Meet the Catalog' },
  ctaSandbox: {
    href: '/infinite-octave-egs-catalog/briefing#portal-sandbox',
    label: 'Open the executive sandbox',
  },
  ctaChat: {
    href: LATTICE_CHAT_DEMO.href,
    label: 'Try Lattice Chat · live demo',
  },
  ctaPaper: {
    href: '/ship-blog/infinite-octave-ai-catalog-layer',
    label: 'Read the note',
  },
  ctaReading: {
    href: '/reading-room?category=lattice-catalog',
    label: 'Reading Room · this category',
  },
  honesty:
    'Catalog architecture for agentic coordination. Not a finished physics proof and not a promise that GPUs disappear. Lattice Chat is a Syntheverse Sandbox live demo — not a production data plane.',
});

/** Plain-language copy constants for editors weaving catalog layer into native surface copy. */
export const LATTICE_CATALOG_LAYER_CONTENT = Object.freeze({
  stackLinePlain:
    'Models & agentic IDEs → Infinite Octave Omniversal Lattice Catalog → ship doors & enterprise data',
  oneLiner:
    'Infinite Octave Omniversal Lattice Catalog is the catalog layer in the AI stack — it sits between models and data so teams ship clearer work and customers only see better answers, without learning the technology underneath.',
  transparencyLine:
    'Executives see control and cost calm. Teams get sharper briefs. Customers only see faster, cleaner answers — they never have to learn Φ or the catalog math.',
  accessLine:
    'Meet the Catalog → try Lattice Chat (Syntheverse Sandbox live demo) → run the EGS executive sandbox → deploy as a mediation layer (handshake · inject · calibrate).',
  honestyShort:
    'Catalog architecture for agentic coordination — not a finished physics proof, and not a promise that GPUs disappear. Lattice Chat is a sandbox demo for evaluators.',
});

/**
 * Security fabric woven through the catalog layer (not a sixth innovation card).
 * Soft Story / architecture grammar — not a penetration-test certificate.
 */
export const CATALOG_LAYER_SECURITY_FABRIC = Object.freeze({
  id: 'security-fabric-24x365',
  title: 'Security fabric · 24×365',
  lead:
    'Security is built into how the catalog files and routes knowledge — not bolted on after the fact.',
  delivers:
    'Keys stay local. Raw corpora stay out of every prompt. The layer indexes schemas as nested filings and routes agents with least privilege across data at rest, in transit, in context windows, in tools, and in executive oversight — always on.',
  how:
    'Cryptographic handshake in a local KMS → catalog container as an interceptor in your cluster → continuous coherence checks. USA-made sovereign enclave path for the CISO when you move from sandbox to production.',
  withoutIt:
    'Without catalog-boundary mediation you keep shipping fat context into every model call and bolting security on as filters — noise, drift, and exposure scale with every agent.',
  honesty:
    'Architecture fabric and deployment path — not a penetration-test certificate or finished ops receipt for every enterprise. Human emergency still outranks algorithms.',
});

/**
 * Five key architectural innovations featured wherever the catalog layer is introduced.
 * Guest labels map to nearest Infinite Octaves corpus shelves (honesty-bound Soft Story).
 * Copy is CEO plain speak — Delivers / Cannot achieve without it.
 */
export const CATALOG_LAYER_INNOVATIONS = Object.freeze([
  Object.freeze({
    id: 'volumetric-interference',
    title: 'Volumetric interference processing',
    guestLabel: 'Separated knowledge vaults · deep context without collision',
    delivers:
      'Files enterprise knowledge into separated vaults so signals do not collide when context gets deep — cleaner recall, less prompt noise.',
    withoutIt:
      'Without it you stay on bloated vector lookup and parity taxes; deep context keeps colliding and burning tokens.',
    href: '/ship-blog/prime-indexed-volumetric-storage',
    hrefLabel: 'Volumetric storage note',
    honestyBite: 'Catalog framing — not a JEDEC drop-in or measured ECC retirement.',
  }),
  Object.freeze({
    id: 'golden-ratio-cataloging',
    title: 'Golden-ratio cataloging',
    guestLabel: 'Φ_EGS ≈ 1.618 · nest order instead of sprawl',
    delivers:
      'Orders nesting with El Gran Sol’s fractal constant (Φ_EGS ≈ 1.618) so context compresses into structure instead of sprawling into more spend.',
    withoutIt:
      'Without it you remain stuck choosing between the brute-force treadmill and the regulatory moat — no third mediation path with structural order.',
    href: '/infinite-octave-egs-catalog',
    hrefLabel: 'EGS Catalog brochure',
    honestyBite: 'Design / catalog key — not a CODATA replacement for ħ, c, or G.',
  }),
  Object.freeze({
    id: 'prime-container-storage',
    title: 'Prime-container storage',
    guestLabel: 'Odd-prime geometry · structure over endless search',
    delivers:
      'Holds structure in odd-prime geometry so hard problems resolve as containment, not endless brute-force search.',
    withoutIt:
      'Without it you only have statistical predictors and longer GPU runs; you cannot file a closed-form containment sketch as architecture.',
    href: '/ship-blog/protein-folding-prime-container',
    hrefLabel: 'Prime-container note',
    honestyBite: 'Architecture Soft Story — not CASP gold, not clinical, not AlphaFold retirement.',
  }),
  Object.freeze({
    id: 'holographic-rhyme-wiring',
    title: 'Holographic rhyme wiring',
    guestLabel: 'Repeating · self-correcting briefs across shelves',
    delivers:
      'Routes agent intent across shelves with repeating, self-correcting briefs — so teams stop re-pasting fat dumps.',
    withoutIt:
      'Without it “holographic” stays a slogan and agents keep pasting archives into every prompt.',
    href: '/ship-blog/holographic-rhyme',
    hrefLabel: 'Holographic rhyme note',
    hrefSecondary: '/ship-blog/tier-c-holographic-wiring',
    hrefSecondaryLabel: 'Tier C wiring',
    honestyBite: 'Catalog motion + wiring grammar — not a claim that prose causes physics.',
  }),
  Object.freeze({
    id: 'goldilocks-net-zero',
    title: 'Goldilocks · Net Zero',
    guestLabel: 'Just right · cool the burn without freezing innovation',
    delivers:
      'Keeps spend, heat, and chaos in the “just right” band — cool the burn without freezing the roadmap. Net-zero calm as an operating target, not a slogan.',
    withoutIt:
      'Without it you swing between thermal and financial runaway on one side and bureaucratic stall on the other — no invariant calm for the enterprise.',
    href: '/ship-blog/holographic-singularity-crystal',
    hrefLabel: 'Net Zero crystal note',
    hrefSecondary: '/ship-blog/zero-octave-y-goldilocks',
    hrefSecondaryLabel: 'Y Goldilocks note',
    honestyBite:
      'Goldilocks / Net Zero as catalog operating grammar — not a claim of measured zero-joule compute or finished CODATA replacement.',
  }),
]);

export const CATALOG_LAYER_INNOVATIONS_HONESTY =
  'Five catalog-layer innovations as Infinite Octaves Soft Story architecture — not finished physics, fab, or JEDEC proofs. Security is woven as 24×365 fabric at the catalog boundary. Human emergency still outranks algorithms.';

/** Compact navy–gold CSS for catalog-layer innovation grids (no purple glow). */
export function renderCatalogLayerInnovationsCss(classPrefix = 'cat-innov') {
  return `
    .${classPrefix} { margin: 1.5rem 0 0; }
    .${classPrefix}__lead { margin: 0 0 1rem; max-width: 42rem; line-height: 1.45; }
    .${classPrefix}__grid {
      display: grid;
      gap: 0.85rem;
      grid-template-columns: 1fr;
    }
    @media (min-width: 720px) {
      .${classPrefix}__grid { grid-template-columns: 1fr 1fr; }
    }
    .${classPrefix}__card {
      margin: 0;
      padding: 0.9rem 1rem 1rem;
      border: 1px solid rgba(201, 162, 39, 0.35);
      background: rgba(8, 18, 40, 0.55);
      border-radius: 2px;
    }
    .${classPrefix}__title {
      margin: 0 0 0.25rem;
      font-size: 1.05rem;
      color: #fff8e7;
      line-height: 1.25;
    }
    .${classPrefix}__label {
      margin: 0 0 0.55rem;
      font-size: 0.72rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: #c9a227;
    }
    .${classPrefix}__delivers,
    .${classPrefix}__without {
      margin: 0 0 0.45rem;
      font-size: 0.9rem;
      line-height: 1.4;
      color: rgba(244, 239, 227, 0.9);
    }
    .${classPrefix}__links { margin: 0.35rem 0; font-size: 0.82rem; }
    .${classPrefix}__links a { color: #c9a227; margin-right: 0.75rem; }
    .${classPrefix}__honesty,
    .${classPrefix}__rail {
      margin: 0.35rem 0 0;
      font-size: 0.72rem;
      line-height: 1.35;
      color: rgba(244, 239, 227, 0.55);
    }
    @media (min-width: 1100px) {
      .${classPrefix}__grid { grid-template-columns: repeat(3, 1fr); }
    }
    .${classPrefix}__security {
      margin: 1.15rem 0 0;
      padding: 0.95rem 1rem 1rem;
      border-left: 3px solid #c9a227;
      background: rgba(8, 18, 40, 0.72);
    }
    .${classPrefix}__security h3 {
      margin: 0 0 0.35rem;
      font-size: 1.05rem;
      color: #fff8e7;
    }
    .${classPrefix}__security p {
      margin: 0 0 0.45rem;
      font-size: 0.9rem;
      line-height: 1.4;
      color: rgba(244, 239, 227, 0.9);
    }
  `.replace(/\n\s+/g, '\n');
}

/** Compact HTML section for guest surfaces (navy–gold; no purple glow). */
export function renderCatalogLayerInnovationsHtml({
  headingId = 'catalog-layer-innovations-h',
  sectionId = 'catalog-layer-innovations',
  classPrefix = 'cat-innov',
  includeSecurity = true,
} = {}) {
  const items = CATALOG_LAYER_INNOVATIONS.map((inn) => {
    const secondary = inn.hrefSecondary
      ? ` <a href="${inn.hrefSecondary}">${inn.hrefSecondaryLabel}</a>`
      : '';
    return `<article class="${classPrefix}__card" id="${inn.id}">
  <h3 class="${classPrefix}__title">${inn.title}</h3>
  <p class="${classPrefix}__label">${inn.guestLabel}</p>
  <p class="${classPrefix}__delivers"><strong>Delivers:</strong> ${inn.delivers}</p>
  <p class="${classPrefix}__without"><strong>Cannot achieve without it:</strong> ${inn.withoutIt}</p>
  <p class="${classPrefix}__links"><a href="${inn.href}">${inn.hrefLabel}</a>${secondary}</p>
  <p class="${classPrefix}__honesty">${inn.honestyBite}</p>
</article>`;
  }).join('\n');

  const S = CATALOG_LAYER_SECURITY_FABRIC;
  const security = includeSecurity
    ? `<aside class="${classPrefix}__security" id="${S.id}" aria-label="${S.title}">
  <h3>${S.title}</h3>
  <p>${S.lead}</p>
  <p><strong>Delivers:</strong> ${S.delivers}</p>
  <p><strong>How:</strong> ${S.how}</p>
  <p><strong>Cannot achieve without it:</strong> ${S.withoutIt}</p>
  <p class="${classPrefix}__honesty">${S.honesty}</p>
</aside>`
    : '';

  return `<!-- CATALOG_LAYER_INNOVATIONS_START -->
<section class="${classPrefix}" id="${sectionId}" aria-labelledby="${headingId}">
  <h2 id="${headingId}">Key architectural innovations</h2>
  <p class="${classPrefix}__lead">What the Infinite Octaves catalog layer files that linear-only stacks cannot — each with what you lose if you skip it. Security is woven through the fabric, not sold as a bolt-on.</p>
  <div class="${classPrefix}__grid">
${items}
  </div>
${security}
  <p class="${classPrefix}__rail">${CATALOG_LAYER_INNOVATIONS_HONESTY}</p>
</section>
<!-- CATALOG_LAYER_INNOVATIONS_END -->`;
}

/** Compact CSS shared by injected bands (navy-gold cruise, not purple glow). */
export function renderLatticeCatalogLayerCss(prefix = 'lat-cat') {
  return `
    .${prefix}-layer {
      position: relative;
      z-index: 4;
      margin: 0;
      padding: 0.85rem 1.15rem 1rem;
      border-bottom: 1px solid rgba(201, 162, 39, 0.35);
      background:
        linear-gradient(105deg, rgba(8, 18, 40, 0.97) 0%, rgba(16, 42, 72, 0.94) 55%, rgba(12, 28, 52, 0.96) 100%);
      color: #f4efe3;
      font-family: "Source Serif 4", "Iowan Old Style", Palatino, Georgia, serif;
    }
    .${prefix}-layer__inner {
      max-width: 68rem;
      margin: 0 auto;
      display: grid;
      gap: 0.45rem 1.25rem;
    }
    @media (min-width: 720px) {
      .${prefix}-layer__inner {
        grid-template-columns: 1fr auto;
        align-items: end;
      }
    }
    .${prefix}-layer__kicker {
      display: inline-block;
      font-family: "DM Sans", "Avenir Next", "Segoe UI", sans-serif;
      font-size: 0.68rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: #c9a227;
      margin: 0 0 0.2rem;
    }
    .${prefix}-layer__title {
      margin: 0;
      font-size: clamp(1.05rem, 2.4vw, 1.35rem);
      font-weight: 600;
      letter-spacing: 0.01em;
      color: #fff8e7;
      line-height: 1.25;
    }
    .${prefix}-layer__lead {
      margin: 0.15rem 0 0;
      font-size: 0.95rem;
      line-height: 1.45;
      color: rgba(244, 239, 227, 0.88);
      max-width: 46rem;
    }
    .${prefix}-layer__stack {
      margin: 0.35rem 0 0;
      font-family: "DM Sans", "Avenir Next", "Segoe UI", sans-serif;
      font-size: 0.78rem;
      letter-spacing: 0.02em;
      color: rgba(201, 162, 39, 0.95);
    }
    .${prefix}-layer__ctas {
      display: flex;
      flex-wrap: wrap;
      gap: 0.45rem 0.65rem;
      align-items: center;
      justify-content: flex-start;
    }
    @media (min-width: 720px) {
      .${prefix}-layer__ctas { justify-content: flex-end; }
    }
    .${prefix}-layer__ctas a {
      font-family: "DM Sans", "Avenir Next", "Segoe UI", sans-serif;
      font-size: 0.78rem;
      letter-spacing: 0.04em;
      text-decoration: none;
      color: #081228;
      background: #c9a227;
      border: 1px solid #c9a227;
      padding: 0.42rem 0.75rem;
      border-radius: 2px;
    }
    .${prefix}-layer__ctas a:hover { background: #e0bc4a; border-color: #e0bc4a; }
    .${prefix}-layer__ctas a.${prefix}-layer__ghost {
      background: transparent;
      color: #f4efe3;
      border-color: rgba(244, 239, 227, 0.45);
    }
    .${prefix}-layer__ctas a.${prefix}-layer__ghost:hover {
      border-color: #c9a227;
      color: #c9a227;
    }
    .${prefix}-layer__honesty {
      grid-column: 1 / -1;
      margin: 0.15rem 0 0;
      font-size: 0.72rem;
      line-height: 1.35;
      color: rgba(244, 239, 227, 0.55);
    }
  `.replace(/\n\s+/g, '\n');
}

/** Full sitewide introduction band (QUESTFEST, Canvas, doors). */
export function renderLatticeCatalogLayerBandHtml({ compact = false } = {}) {
  const L = LATTICE_CATALOG_LAYER;
  const lead = compact
    ? 'New AI stack category — coordination fabric over the cruise doors, not a sixth door.'
    : L.lead;
  return `<!-- LATTICE_CATALOG_LAYER_START -->
  <aside class="lat-cat-layer" id="ai-catalog-layer" aria-label="${L.kicker}">
    <div class="lat-cat-layer__inner">
      <div class="lat-cat-layer__copy">
        <p class="lat-cat-layer__kicker">${L.kicker}</p>
        <p class="lat-cat-layer__title">${L.title}</p>
        <p class="lat-cat-layer__lead">${lead}</p>
        <p class="lat-cat-layer__stack">${L.stackLine}</p>
      </div>
      <div class="lat-cat-layer__ctas">
        <a href="${L.ctaPrimary.href}">${L.ctaPrimary.label}</a>
        <a class="lat-cat-layer__ghost" href="${L.ctaSandbox.href}">${L.ctaSandbox.label}</a>
        <a class="lat-cat-layer__ghost" href="${L.ctaChat.href}">${L.ctaChat.label}</a>
        <a class="lat-cat-layer__ghost" href="${L.ctaPaper.href}">${L.ctaPaper.label}</a>
      </div>
      <p class="lat-cat-layer__honesty">${L.honesty}</p>
    </div>
  </aside>
  <!-- LATTICE_CATALOG_LAYER_END -->`;
}

/** Marker pair used by sync scripts. */
export const LATTICE_CATALOG_LAYER_MARKERS = Object.freeze({
  start: '<!-- LATTICE_CATALOG_LAYER_START -->',
  end: '<!-- LATTICE_CATALOG_LAYER_END -->',
  styleStart: '<!-- LATTICE_CATALOG_LAYER_STYLE_START -->',
  styleEnd: '<!-- LATTICE_CATALOG_LAYER_STYLE_END -->',
});
