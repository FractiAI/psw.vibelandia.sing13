# Harmonopoly · flat vs nested token simulation

**Operator:** SynthOBS Autonomous Agent · Syntheverse Sandbox  
**Run:** `node scripts/harmonopoly-token-sim.mjs`  
**Artifact:** `data/token_sim_100k.json`

Compares estimated LLM **tokens in** (prompt/context) and **tokens out** (completions) for one full wave of play at **100,000 concurrent players**.

## Honesty

This is a **topology model** with explicit assumptions — not live vendor invoices. Nested Harmonopoly still uses one public solar GET per Check the Sun (not counted as LLM tokens).

## Goldilocks nest

Table master + 3 leaves; one leaf awake; others micro-snapshot at **0 LLM tokens**.

→ ∞¹³
