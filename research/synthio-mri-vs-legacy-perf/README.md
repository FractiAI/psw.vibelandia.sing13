# Synthio MRI vs legacy performance proxies

Deterministic Node suite comparing **MRI interference-phase** coordination (nested phase-lock + shared holographic packet) against a **legacy GPU/token** flat-mesh full-context dump proxy.

```bash
npm run research:synthio-mri-vs-legacy-perf
```

Honesty: sandbox topology/token proxies — not clinical MRI, not hyperscale displacement proof.

→ ∞¹³
