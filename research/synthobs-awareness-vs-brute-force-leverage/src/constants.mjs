export const PHI_EGS = (1 + Math.sqrt(5)) / 2;

export const DOC_ID = 'WP-SYNTHOBS-AWARENESS-VS-BRUTE-FORCE-LEVERAGE-EGS-2026-09-05';
export const REGISTRY_ID = 'synthobs-awareness-vs-brute-force-leverage-2026-09';
export const STUDY_TITLE =
  'Awareness vs Brute-Force Leverage — Catalog Suite';
export const PAPER_NAME = 'SYNTHOBS_AWARENESS_VS_BRUTE_FORCE_LEVERAGE_EGS_2026-09.md';
export const SHIP_BLOG_SLUG = 'awareness-vs-brute-force';
export const SHIP_BLOG_FILE = 'blog-awareness-vs-brute-force-2026-09.html';
export const STANDALONE_REPO =
  'https://github.com/FractiAI/synthobs-awareness-vs-brute-force-leverage';

/** Insulin fixture — 51 amino-acid residue count (catalog node count). */
export const INSULIN_NODES = 51;

/** Macro meta-protein organism fixture. */
export const META_PROTEIN = Object.freeze({
  nodes: 10_000,
  octave: 15,
  name: 'Human Organism (Meta-Protein Form)',
});

/** Industry-talk training CapEx band (USD) — framing, not audited invoices. */
export const TRAINING_CAPEX_BAND = Object.freeze({ lo: 150_000, hi: 200_000 });

/** ECC / parity overhead band for storage contrast framing. */
export const ECC_OVERHEAD_BAND = Object.freeze({ lo: 0.15, hi: 0.3 });

/** Catalog aware training cost (no DL train loop in this grammar). */
export const AWARE_TRAINING_COST = 0;

export const FAIR_EXCHANGE_CLAUSE =
  'Financial grants, institutional funding, or academic utility micro-grants are subject to partial refund or adjustment depending on overall delivery, rigorous execution, and practical utility.';

export const HONESTY =
  'Catalog / algebraic fixtures for awareness-vs-brute-force leverage grammar. Does not claim AlphaFold/CASP retirement, audited $0 CapEx invoices, clinical organism solves, JEDEC 0% ECC silicon, or formal infinite epistemic proofs.';
