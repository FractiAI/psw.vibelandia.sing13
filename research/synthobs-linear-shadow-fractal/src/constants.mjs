export const PHI_EGS = (1 + Math.sqrt(5)) / 2;

export const DOC_ID = 'WP-SYNTHOBS-LINEAR-SHADOW-FRACTAL-2026-09-19';
export const REGISTRY_ID = 'synthobs-linear-shadow-fractal-2026-09';
export const STUDY_TITLE =
  'Linear as the Shadow of Fractal — Projection Geometry (Catalog Suite)';
export const PAPER_NAME = 'SYNTHOBS_LINEAR_SHADOW_FRACTAL_EGS_2026-09.md';
export const SHIP_BLOG_SLUG = 'linear-shadow-fractal';
export const SHIP_BLOG_FILE = 'blog-linear-shadow-fractal-2026-09.html';
export const STANDALONE_REPO = 'https://github.com/FractiAI/synthobs-linear-shadow-fractal';

/** Cast role — one-axis silhouette. */
export const CAST_ROLE = 'linear';

/** Body role — recursive nesting architecture. */
export const BODY_ROLE = 'fractal_holographic';

/** Projection relation Soft Story. */
export const PROJECTION_RELATION = 'cast = projection_of(body)';

/** Linear AI-race silhouette steps (catalog). */
export const LINEAR_SILHOUETTE = Object.freeze([
  'flops_axis',
  'cluster_scale',
  'market_share',
  'single_axis_risk',
]);

/** Keep CMOS / linear engineering instruments. */
export const INSTRUMENT_LOCK = Object.freeze({
  linearInstrumentsRetained: true,
  discardLinearTooling: false,
});

export const HONESTY =
  'Catalog / Soft Story fixtures for linear-as-shadow-of-fractal. Does not claim physics is a cave wall, that linear algebra is obsolete, that Φ_EGS replaces CODATA, or prophecy about AGI dates.';
