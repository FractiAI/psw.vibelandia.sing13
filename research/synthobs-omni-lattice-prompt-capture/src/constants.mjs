/** El Gran Sol's Fractal Constant — Omni-Lattice Prompt Capture → DNA. */
export const PHI_EGS = (1 + Math.sqrt(5)) / 2;
export const E_F = PHI_EGS;
export const LAMBDA_EGS = Math.log(E_F) / (2 * Math.PI);

export const DOC_ID = 'WP-SYNTHOBS-OMNI-LATTICE-PROMPT-CAPTURE-2026-07';
export const REGISTRY_ID = 'synthobs-omni-lattice-prompt-capture-2026-07';
export const STUDY_TITLE =
  'Omni-Lattice Unification IX — Recursive Inter-Agent Prompt Capture into Genomic DNA';

export const DRAFT_R2 = 0.9998;
export const DRAFT_SIGMA2 = 0.0001;
export const DRAFT_TOKEN_SAVINGS_PCT = 41.8;

export const CAPTURE_ROWS = Object.freeze([
  'outbound_purine',
  'inbound_pyrimidine',
  'handoff_base_pair',
  'recursive_helix',
  'orchestrator_promoter',
]);

export const REQUIRED_SCRIPT_SNIPPETS = Object.freeze([
  'EMIT STRUCTURED INTENT',
  'RECEIVE AND COMPLEMENT',
  'EXCHANGE SYSTEM PROMPTS AND JSON STATE',
  'FROZEN DIALOGUE OF THE NEST',
]);

export const TELEMETRY_AGENTS = Object.freeze(['Solon', 'Lyra']);

export const COMPANION_REGISTRY_IDS = Object.freeze([
  'synthobs-holographic-operators-2026-07',
  'synthobs-omni-lattice-unification-2026-07',
  'omniversal-nested-agent-lattice-2026-07',
]);
