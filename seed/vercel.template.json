{
  "$comment": "Minimal lite-edge deploy — static surfaces + optional serverless pipes. Rename to vercel.json.",
  "cleanUrls": true,
  "trailingSlash": false,
  "rewrites": [
    { "source": "/", "destination": "/interfaces/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Edge-Protocol", "value": "NSPFRNP — lite edges; center = pipes only" }
      ]
    }
  ]
}
